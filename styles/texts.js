import { StyleSheet } from "react-native";

const texts = StyleSheet.create({
  header: {
    fontSize: 30,
    fontWeight: "bold",
  },
  searchText: {
    fontSize: 15,
    flex: 1,
  },
  flatListText: {
    fontSize: 15,
  },
});

export default texts;
